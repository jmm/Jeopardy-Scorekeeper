These are experimental tests, relying on a local dependency that isn't checked in, of validating component propTypes. If they become not experimental they can be moved to somewhere in unit/component.
