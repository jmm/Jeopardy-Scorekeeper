"use strict";

var ReactTestUtils = require("react-addons-test-utils");

module.exports = reactShallowRender;

function reactShallowRender (element) {
  return ReactTestUtils.createRenderer().render(element);
}
